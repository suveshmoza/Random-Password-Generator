import React from "react";

const Generator = () => {
  return (
    <div className="password">
      <input type="text" />
      <button>Generate</button>
    </div>
  );
};

export default Generator;
